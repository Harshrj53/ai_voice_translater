# ─── Hindi Video Dubbing Pipeline — Auto Install Requirements ────────────────

# Core FFmpeg wrapper
ffmpeg-python>=0.2.0

# ── Speech Recognition (Whisper)
openai-whisper>=20231117
tiktoken>=0.7.0
numba>=0.59.0

# ── Translation
transformers>=4.38.0
sentencepiece>=0.1.99
sacremoses>=0.1.1
deep-translator>=1.11.4

# ── Text-to-Speech (Auto fallback safe)
gTTS>=2.5.0

# Optional voice cloning (only installs if supported Python)
TTS>=0.22.0 ; python_version < "3.12"

# ── Audio Processing
librosa>=0.10.1
soundfile>=0.12.1
pydub>=0.25.1
scipy>=1.11.0
audioread>=3.0.0

# ── Video / Image Processing
opencv-python>=4.9.0.80

# Face enhancement (auto skip if unsupported Python)
gfpgan>=1.3.8 ; python_version < "3.12"
basicsr>=1.4.2 ; python_version < "3.12"
facexlib>=0.3.0 ; python_version < "3.12"
realesrgan>=0.3.0 ; python_version < "3.12"

# ── PyTorch
torch>=2.0.0
torchvision>=0.15.0

# ── Utilities
gdown>=4.7.3
numpy>=1.24.0
tqdm>=4.66.0
Pillow>=10.0.0
imageio>=2.34.0
imageio-ffmpeg>=0.4.9
requests>=2.31.0

# Notebook / UI (optional)
ipywidgets>=8.0.0